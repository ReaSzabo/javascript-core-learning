// #4
// Write an arrow function that returns the square of a number. (square = négyzet)
// E.g parameter is 3, return value is 9 (3*3).
// E.g parameter is 4, return value is 16 (4*4).

var getSquare = (number) => {
    return number ** 2;
}

console.log(getSquare(2))