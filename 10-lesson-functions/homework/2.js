console.log("Exercise 2");
// Create a function that has 2 parameters.
// x and y
// The function should log the product of the 2 numbers
// (product = szorzat)

function numbers(x, y) {
    console.log(x * y)
}

numbers(2, 3);